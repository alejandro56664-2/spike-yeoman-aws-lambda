// dependencies: repository-dynamodb
dependencies {
    implementation(group: 'software.amazon.awssdk', name: 'dynamodb', version: "${awsDynamoDBVersion}")
    implementation(group: 'software.amazon.awssdk', name: 'dynamodb-enhanced', version: "${awsDynamoDBVersion}")
}
